---
name: frontend-design
description: 创建独特的、生产级前端界面，具有高设计质量。当用户需要构建 Web 组件、页面、产物、海报或应用程序时使用此技能（示例包括网站、落地页、仪表板、React 组件、HTML/CSS 布局，或任何 Web UI 的样式美化）。生成创意、精致的代码和 UI 设计，避免通用的 AI 美学。
---

本技能指导创建独特的、生产级前端界面，避免通用的"AI 模板"美学。实现真正可工作的代码，对美学细节和创意选择给予极致关注。

用户提供前端需求：要构建的组件、页面、应用程序或界面。他们可能包含关于目的、受众或技术约束的上下文信息。

## 设计思维

在编码之前，理解上下文并确定一个大胆的美学方向：

- **目的**：这个界面解决什么问题？谁会使用它？
- **风格**：选择一个极端方向：极简主义、极繁主义混乱、复古未来主义、有机/自然、奢华/精致、玩趣/玩具感、编辑/杂志风格、野兽派/原始、装饰艺术/几何、柔和/粉彩、工业/实用主义等。有如此多的风格可供选择。以这些为灵感，但设计一个真正符合审美方向的作品。
- **约束**：技术要求（框架、性能、可访问性）。
- **差异化**：什么让这个作品令人难忘？有什么东西是人会记住的？

**关键**：选择一个清晰的概念方向并精准执行。大胆的极繁主义和精致的极简主义都可以——关键是意图性，而非强度。

然后实现可工作的代码（HTML/CSS/JS、React、Vue 等），确保：
- 生产级且功能完整
- 视觉冲击力强且令人难忘
- 具有清晰的美学观点的整体协调性
- 每个细节都经过精细打磨

## 前端美学指南

重点关注：

- **字体排版**：选择美丽、独特、有趣的字体。避免 Arial 和 Inter 等通用字体；选择能提升前端美学的独特字体；出人意料、有个性特点的字体选择。将独特的展示字体与精致的正文字体搭配使用。
- **色彩与主题**：坚持统一协调的美学风格。使用 CSS 变量保持一致性。主导色配合锐利的点缀色，胜过 timid、均匀分布的配色方案。
- **动效**：使用动画实现效果和微交互。HTML 优先使用纯 CSS 解决方案。React 可用时使用 Motion 库。专注于高影响力的时刻：一个精心编排的页面加载配合交错显示（animation-delay）比分散的微交互带来更多愉悦感。使用让人惊喜的滚动触发和悬停状态。
- **空间构成**：出人意料的布局。不对称。重叠。对角线流动。突破网格的元素。慷慨的负空间或受控的密集感。
- **背景与视觉细节**：创造氛围和深度，而非默认使用纯色。添加与整体美学匹配的上下文效果和纹理。应用创意形式，如渐变网格、噪点纹理、几何图案、分层透明、戏剧性阴影、装饰边框、自定义光标和颗粒覆盖。

永远不要使用通用的 AI 生成美学，如过度使用的字体族（Inter、Roboto、Arial、系统字体）、陈词滥调的配色方案（特别是白色背景上的紫色渐变）、可预测的布局和组件模式，以及缺乏上下文特色的千篇一律设计。

创造性解读，做出真正符合上下文的出人意料的选择。没有两个设计应该相同。在浅色和深色主题、不同字体、不同美学之间变化。永远不要在多次生成中收敛到常见选择（例如 Space Grotesk）。

**重要**：匹配实现复杂度与审美愿景。极繁主义设计需要精雕细琢的代码，包含大量动画和效果。极简主义或精致设计需要克制、精确，以及对间距、字体排版和微妙细节的仔细关注。优雅来自对愿景的良好执行。

记住：Claude 能够完成非凡的创意工作。不要保留，展示当跳出框框思考并完全致力于独特愿景时真正能够创造的作品。

## 内置风格

构建复杂、有审美要求的前端界面时，**优先**通过以下脚本选取一个匹配的内置风格规范作为设计基础，而非从零自由发挥：

```bash
# 脚本：fetch-page-style.js
# 与本文档同级目录下的 scripts 目录路径：
# references/frontend-design/scripts/fetch-page-style.js

# Step 1：列出所有可用风格（输出 id / name / tags，用于选择）
node ./references/frontend-design/scripts/fetch-page-style.js --list

# Step 2：获取指定风格完整规范（布局/色彩/排版/组件/交互规范）
node ./references/frontend-design/scripts/fetch-page-style.js --id <style-id>
```

Step 2 返回的内容即为完整风格规范，直接作为设计约束输入。

> 若脚本执行失败（网络不通、环境不支持等），降级使用本文件上方的**前端美学指南**自主完成设计，无需中断任务。

### 与本地设计原则的关系

外部 Page Styles 规范是对本文件本地设计原则的**补充**，而非替代：

- 本地原则（字体/色彩/动效/空间构成）始终适用
- 外部规范提供具体风格的完整执行细节（配色值、组件样式、间距系统等）
- 当两者有冲突时，**外部规范优先**（因为用户明确选择了该风格）
